<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Notification permission — required for Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Boot completed — reschedule alarms after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Wake lock — ensures BroadcastReceiver completes async work -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Exact alarms — SCHEDULE_EXACT_ALARM is the correct permission for health/diet
         reminder apps. The scheduler checks canScheduleExactAlarms() at runtime and
         falls back to inexact if not yet granted (M2 fix). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NourishCycle">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Meal alarm receiver — fires at exact meal times -->
        <receiver
            android:name=".notification.MealNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.wefit.nourishcycle.MEAL_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver — exported=true required for system BOOT_COMPLETED delivery.
             The intent-filter restricts it to system-only broadcast actions (M3 fix). -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
