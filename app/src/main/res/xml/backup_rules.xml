<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude Room database (health/personal data — user should control reset) -->
    <exclude domain="database" path="nourishcycle_db" />
    <exclude domain="database" path="nourishcycle_db-shm" />
    <exclude domain="database" path="nourishcycle_db-wal" />
    <!-- Include DataStore preferences (cycle start date is safe to back up) -->
</full-backup-content>
